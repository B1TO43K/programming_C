#include <stdio.h> 

int main() { 

  double a[3][3], sum1 = 0, sum2 = 0; // - a[3][3]: ìàññèâ double 3x3 / sum1/sum2: ñóììà ãëàâíîé/ïîáî÷íîé äèàãîíàëè (èçíà÷àëüíî 0)
                                     
  int i, j;                       // Îáúÿâëÿåì ïåðåìåííûå äëÿ öèêëîâ

  printf("Enter 9 numbers for the array double:\n"); 
  for (i = 0; i < 3; i++)                      // Öèêë ïî ñòðîêàì (0, 1, 2)
    for (j = 0; j < 3; j++)                    // Öèêë ïî ñòîëáöàì (0, 1, 2)
      scanf("%lf", &a[i][j]);                   // Ñ÷èòûâàåì double è ñîõðàíÿåì â a[i][j]

  for (i = 0; i < 3; i++) {                     // Öèêë ïî ãëàâíîé è ïîáî÷íîé äèàãîíàëÿì
    sum1 += a[i][i];                              // Ñ÷èòàåì ñóììó ãëàâíîé äèàãîíàëè (a[0][0] + a[1][1] + a[2][2])
    sum2 += a[i][2 - i];                          // Ñ÷èòàåì ñóììó ïîáî÷íîé äèàãîíàëè (a[0][2] + a[1][1] + a[2][0])
  }

  printf("The sum of the main diagonal: %.2lf\n", sum1);   // Âûâîäèì ñóììó ãëàâíîé äèàãîíàëè
  printf("The sum of the side diagonal: %.2lf\n", sum2);   // Âûâîäèì ñóììó ïîáî÷íîé äèàãîíàëè

  int b[2][2], c[2][2];            // Îáúÿâëÿåì:
                                      // - b[2][2]: ìàññèâ int 2x2 (èñõîäíàÿ ìàòðèöà)
                                      // - c[2][2]: ìàññèâ int 2x2 (êâàäðàò ìàòðèöû)
  printf("\nEnter 4 numbers for the array int:\n");
  for (i = 0; i < 2; i++)                      // Öèêë ïî ñòðîêàì (0, 1)
    for (j = 0; j < 2; j++)                    // Öèêë ïî ñòîëáöàì (0, 1)
      scanf("%d", &b[i][j]);                   // Ñ÷èòûâàåì int è ñîõðàíÿåì â b[i][j]

  c[0][0] = b[0][0] * b[0][0] + b[0][1] * b[1][0]; // Âû÷èñëÿåì ýëåìåíò c[0][0] êâàäðàòà ìàòðèöû
  c[0][1] = b[0][0] * b[0][1] + b[0][1] * b[1][1]; // Âû÷èñëÿåì ýëåìåíò c[0][1] êâàäðàòà ìàòðèöû
  c[1][0] = b[1][0] * b[0][0] + b[1][1] * b[1][0]; // Âû÷èñëÿåì ýëåìåíò c[1][0] êâàäðàòà ìàòðèöû
  c[1][1] = b[1][0] * b[0][1] + b[1][1] * b[1][1]; // Âû÷èñëÿåì ýëåìåíò c[1][1] êâàäðàòà ìàòðèöû

  printf("\nThe square of the matrix:\n");       
  for (i = 0; i < 2; i++) {                     // Öèêë ïî ñòðîêàì ðåçóëüòèðóþùåé ìàòðèöû
    for (j = 0; j < 2; j++) {                   // Öèêë ïî ñòîëáöàì ðåçóëüòèðóþùåé ìàòðèöû
      printf("%d ", c[i][j]);                   // Âûâîäèì ýëåìåíò c[i][j]
    }
    printf("\n");                             
  }

  return 0; 
}
